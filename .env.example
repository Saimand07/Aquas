# ==============================================================================
# AQUAS · PRIVACY-PRESERVING ZERO-KNOWLEDGE MEDICAL LICENSE REGISTRY
# Midnight Network Environment Configuration
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. CORE MIDNIGHT SMART CONTRACT
# ------------------------------------------------------------------------------
# Address of the deployed DoctorLicense Compact smart contract on Midnight Preview Testnet.
# Verified on-chain contract address:
NEXT_PUBLIC_CONTRACT_ADDRESS="0xd5e2dc450d37260f6f43d4b15ab74f48e91dfd81497735506e27c0c3257d9b74"

# ------------------------------------------------------------------------------
# 2. MIDNIGHT NETWORK & 1AM GATEWAY ENDPOINTS (OPTIONAL / AUTO-CONFIGURED BY 1AM)
# ------------------------------------------------------------------------------
# GraphQL Indexer endpoint for querying public ledger states and nullifiers
NEXT_PUBLIC_INDEXER_URI="https://api-preprod.1am.xyz/api/v4/graphql"

# WebSocket Indexer endpoint for real-time telemetry streaming
NEXT_PUBLIC_INDEXER_WS_URI="wss://api-preprod.1am.xyz/api/v4/graphql/ws"

# Midnight RPC Node endpoint
NEXT_PUBLIC_NODE_URI="wss://rpc.preprod.midnight.network"

# ------------------------------------------------------------------------------
# 3. ENTERPRISE HOSPITAL EHR & WEBHOOKS GATEWAY (PHASE 4)
# ------------------------------------------------------------------------------
# Secret Bearer API Key for hospital EHR (Epic / Cerner) machine-to-machine calls
AQUAS_EHR_API_KEY="aq_live_hospital_secret_key_demo"

# Secret HMAC signing key for outbound regulatory webhook events
AQUAS_WEBHOOK_SECRET="whsec_midnight_aquas_regulatory_audit_stream"
